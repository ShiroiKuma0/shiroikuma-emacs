# CLAUDE.md — guide for Claude Code in this repo

**shiroikuma-emacs** — 白い熊's fork of [GNU Emacs](https://github.com/emacs-mirror/emacs) `master`
(the byte-identical GitHub mirror of Savannah; C + Java + Elisp; GPL-3.0-or-later), built for
**Android** the way the SourceForge `termux/` packages are. Installed as `shiroikuma.emacs` /
**白い熊 GNU Emacs**, side-by-side with the stock `org.gnu.emacs`, and sharing Termux's user ID
(`com.termux`) so every CLI tool in the Termux prefix is reachable from Emacs.

This repo (`ShiroiKuma0/shiroikuma-emacs`) is a fork. We track upstream's **branch tip** on
`master` and layer our customizations on `custom`.

**Upstream's `AGENTS.md`** (the file this `CLAUDE.md` used to point at) states GNU Emacs's policy
against LLM-generated *contributions to Emacs*. Nothing from this repo is ever submitted upstream —
the `upstream` remote's push URL is `DISABLED` — so it does not govern work on the fork layer.
Leave `AGENTS.md` as upstream has it.

## Read this first

Before any work, read **`.claude/skills/build-apk/SKILL.md`** (canonical build + delivery) and
**`.claude/skills/upstream-new-version/SKILL.md`** (upstream sync + rebase, with the mandatory
proceed-gated upstream-changes table). Publishing a release uses the **global** `/publish-version`
skill — this repo has no local copy.

## Fork workflow — READ THIS FIRST

### Git remotes & branches

- `origin` → `git@github.com:ShiroiKuma0/shiroikuma-emacs.git` (push here).
- `upstream` → `https://github.com/emacs-mirror/emacs.git` (fetch only; its push URL is `DISABLED`).
- `master` — mirrors `upstream/master`, **fast-forward only**. No fork work here.
- `custom` — all our work, rebased onto `master` on each sync, and the GitHub default branch so the
  repo page lands on the fork.

**Upstream tracking: `git`** — `custom` is rebased onto every upstream commit, so the fork
versionName pins the upstream base: `<upstream>+<base date>.<HH-MM>.g<sha>+<BUILD_NUMBER, 3 digits>`.
See the global **`git-versioning`** skill. Why: Emacs `master` is what SourceForge builds and what
白い熊 installs; it takes hundreds of commits a month while `AC_INIT`'s `32.0.50` stands still for
the whole development cycle, so the literal alone says nothing about how current the fork is.

### Our customizations (install identity + build)

| What | Value | Where |
| --- | --- | --- |
| applicationId | `shiroikuma.emacs` — via aapt `--rename-manifest-package` at packaging time | `java/Makefile.in` → `FORK_APPLICATION_ID` (the `# --- shiroikuma fork ---` block) |
| Java namespace | `org.gnu.emacs` (**never rename** — every JNI class path in `src/android*.c` depends on it) | `java/AndroidManifest.xml.in` `package=`, `java/org/gnu/emacs/` |
| sharedUserId | `com.termux` (same UID as Termux; needs the same signing key) | `./configure --with-shared-user-id=com.termux` in `build-fork.sh` |
| `EmacsConfig.APPLICATION_ID` | the installed id, for code that must not hard-code `org.gnu.emacs` | generated by `java/Makefile.in` (`cf-stamp-1`) |
| App label | `白い熊 GNU Emacs` (`@string/shiroikuma_app_name`); options activities `白い熊 GNU Emacs UI` (`shiroikuma_ui_title`); service `白い熊 GNU Emacs service` (`shiroikuma_service_name`) | `java/res/values/shiroikuma_strings.xml` (ours; upstream's `strings.xml` untouched), referenced from `java/AndroidManifest.xml.in` (`<application>`, `.EmacsPreferencesActivity`, `.EmacsLauncherPreferencesActivity`, `EmacsService`) |
| App icon | black-yellow traced gnu-E: `@mipmap/shiroikuma_icon` (app), `@mipmap/shiroikuma_ui_icon` (UI entry), `R.drawable.shiroikuma` (picker sidebar), `R.drawable.shiroikuma_notification` (service notification) | `java/res/mipmap-v26/shiroikuma_{icon,ui_icon}.xml`, `java/res/mipmap/shiroikuma_{icon,ui_icon}.png`, `java/res/drawable/shiroikuma{,_background,_foreground,_ui_foreground,_notification}.{png,xml}` — generated by `tools/icon/emit_launcher.py` from `design/*.svg` (edit the model, not the files); upstream's `emacs_icon.*` / `emacs.png` / `emacs_wrench.png` stay in the tree, unreferenced |
| 白い熊 GNU Emacs UI page | `org.gnu.emacs.shiroikuma.ShiroikumaUiActivity` (plain `Activity`, programmatic views, house look, theme `ShiroikumaStyle`): Export / Import · Emacs options (the three stock rows re-implemented — `EmacsPreferencesActivity.java` / `res/xml/preferences.xml` stay compiled but unreferenced) · Appearance · Reset. Opens from the 白い熊 GNU Emacs UI launcher entry (`EmacsLauncherPreferencesActivity extends ShiroikumaUiActivity`), from Settings › App info › App settings (`APPLICATION_PREFERENCES`), and from a long-press on the Emacs icon (static shortcut) | `java/org/gnu/emacs/shiroikuma/{ShiroikumaUiActivity,ShiroikumaLook,ShiroikumaPrefs}.java`, `java/res/values/shiroikuma_style.xml`, `java/res/drawable/shiroikuma_dialog_bg.xml`, `java/res/xml/shortcuts.xml`, strings in `shiroikuma_strings.xml`; upstream hooks: `EmacsLauncherPreferencesActivity.java` (the `extends`), `EmacsActivity.java` (`ShiroikumaPrefs.applyWindowPrefs (this)` after `setContentView`), `AndroidManifest.xml.in` (the two activities' class/theme, the `android.app.shortcuts` meta-data on `EmacsActivity`) |
| Export / Import (+ tar backup of ~) | `ExportImportPanel` (the family panel) over the `EmacsBackup` engine: one `shiroikuma-emacs_<yyyy-MM-dd_HH-mm-ss>.zip` written as `.part` and renamed on completion; `manifest.json` first, `settings.json`, `data.home.tar` (DEFLATE `BEST_SPEED`, in-tree `TarWriter`/`TarReader`: ustar + GNU LongLink, `Os.lstat` modes/mtimes, symlinks, sockets skipped, `files/emacs-*.pdmp` excluded). Import while Emacs runs = spool + `pending_import` marker + relaunch of the page with `System.exit(0)` (upstream's own restart trick), restore in the fresh process | `java/org/gnu/emacs/shiroikuma/{ExportImportPanel,EmacsBackup,TarWriter,TarReader,SafDir}.java` |
| Automation (sister-app contract v2) | **Done (Phase 4a + 4b).** The three rows on the page + `AutomationAuth` (prefs `shiroikuma_automation`); the §1 door `StateExportReceiver` (`shiroikuma.emacs.action.EXPORT_STATE` / `LIST_CATEGORIES` / `CANCEL_EXPORT`, exported, no permission); the §2a door `AutomationProvider` (authority `shiroikuma.emacs.automation`, `describe` / `export` / `import` / `cancel`, caller pinned by `AutomationCallers`); both run in `BackupService` (`dataSync`, unexported); `AutomationJobs` (job ids + cancel flags), `AutomationProgress` (the one §3 sender); manifest `<queries>` for both callers + the three `shiroikuma.automation.*` `<meta-data>` ints (contract 2, format 1, min_format 1) | `java/org/gnu/emacs/shiroikuma/{AutomationAuth,AutomationCallers,AutomationJobs,AutomationProgress,StateExportReceiver,AutomationProvider,BackupService}.java`, `java/AndroidManifest.xml.in` (the `shiroikuma fork: … automation contract v2` block after `EmacsDocumentsProvider`, the `<queries>` before `<application>`), `shiroikuma_strings.xml` (`shiroikuma_backup_channel`, `shiroikuma_auto_notif_*`) |
| Version tail | `versionName = "<AC_INIT>+<pin>+NNN"`, `versionCode = <Version-code trailer> + N` | `build-fork.sh` → `make FORK_VERSION_NAME= FORK_VERSION_CODE=`; counter in `fork.properties` |
| Signing | gitignored `keystore.properties` → `~/.android-keystores/shiroikuma-emacs-termux.jks` (alias `Emacs keystore`, with the space) | `build-fork.sh` exports `FORK_KEYSTORE*`; `java/Makefile.in` `SIGN_EMACS` / `SIGN_EMACS_V2` |
| Fork links | `https://github.com/ShiroiKuma0/shiroikuma-emacs` (+ `/releases`) | root `README` fork header (prepended above upstream's text), `java/res/values/shiroikuma_strings.xml` header; the app itself shows no repo URL anywhere (upstream has none either) |
| De-branding | our name + our icon everywhere user-visible; upstream copyright/attribution, code namespaces, log tags, intent actions/extras untouched | `java/AndroidManifest.xml.in` (labels, icons, `android:authorities="shiroikuma.emacs"`), `java/res/values/shiroikuma_strings.xml`, `EmacsDocumentsProvider.java` (root title/summary/icon), `EmacsService.java` (channel name, title, small icon), `README` |
| Installed-id patch list | every runtime use of the package name follows the **installed** id, never the namespace — the fix for `INSTALL_FAILED_CONFLICTING_PROVIDER` and for the rename's silent misbehaviours | `EmacsApplication.java` (`getPackageName ()`), `EmacsNoninteractive.java` (`EmacsConfig.APPLICATION_ID` — no Context yet), `EmacsDesktopNotification.java` (`context.getPackageName ()` for `RemoteViews` / `setPackage`), `EmacsDocumentsProvider.java` (`buildChildDocumentsUri` authority), `EmacsService.java` (`buildDocumentUri` authority, `package:` settings URI), `EmacsWindowManager.java` (`getClassName ()` vs `EmacsMultitaskActivity.class.getName ()` — `getShortClassName ()` only abbreviates inside the manifest package) |

Upstream's own literals are **read, never edited**: `AC_INIT([GNU Emacs], [32.0.50] …)` in
`configure.ac`, the manifest's placeholder `android:versionCode="30"` and its
`Version-code: 320050000` trailer comment (upstream's reserved F-Droid slot for this version,
maintained by `admin/admin.el`). `--rename-manifest-package` only rewrites `package=` and
relative class names — the DocumentsProvider authority and every `"org.gnu.emacs"` runtime
literal in Java are patched by hand (the "Installed-id patch list" row), which is what makes the
APK installable **beside** the stock Emacs. The three `org.gnu.emacs` strings that remain are
names, not the package: the `org.gnu.emacs.DISMISSED` intent action (matches the manifest's
receiver filter), the `org.gnu.emacs.STARTUP_ARGUMENTS` intent extra, and the
`org.gnu.emacs.EmacsNoninteractive` class name loaded by reflection.

### Versioning & APK naming

- `BUILD_NUMBER` (in `fork.properties`) is our per-build `N`:
  `versionName = "<AC_INIT>+<YYYY-MM-DD>.<HH-MM>.g<sha8>+<NNN>"` (e.g.
  `32.0.50+2026-09-13.02-35.gf0430371+001`), `versionCode = 320050000 + N` (plain integer,
  e.g. `320050001`). Zero-padded to 3 digits **in the name only**.
- The pin is `git merge-base HEAD master` (the upstream commit our patches sit on — not our HEAD,
  not `master`'s tip) shortened to 8 chars, plus that commit's committer date **and time, in UTC**.
  It moves only on a sync.
- **`versionCode` base is the `Version-code:` trailer, not `30 × 10000`.** The manifest's
  `versionCode="30"` is a placeholder upstream deliberately never increments (to allow downgrades),
  and `320050000 × 10000` overflows `int`. `Version-code + N` is what upstream itself reserves for
  F-Droid and it sits above every stock build of this version.
- **`BUILD_NUMBER` runs MONOTONICALLY. Reset it to `1` ONLY when the `Version-code:` base itself
  moves** (i.e. upstream bumps `AC_INIT` — once per major version) — never merely because a sync
  moved the `.g<sha>` pin. An installer compares `versionCode` and nothing else; resetting `N` on a
  sync would send it *backwards*. `build-fork.sh` records `LAST_BUILT_VERSION_CODE` in
  `fork.properties` and **refuses to build** a code that does not exceed it.
- APK: `shiroikuma-emacs_<versionName>_arm64-v8a.apk`, copied to `~/tmp/` (never overwritten —
  raise `BUILD_NUMBER` instead). Upstream's own output stays in `java/` as
  `emacs-<ver>-29-arm64-v8a.apk` (the `29` is `ANDROID_MIN_SDK`, taken from the compiler name
  `aarch64-linux-android29-clang`). The versionName contains no `_` and no `~` (Debian and
  `git check-ref-format` bans, see the `git-versioning` skill).

### Build commands

```bash
# Our build: signed release APK → ~/tmp + bump BUILD_NUMBER (use this; ~20 min cold → background)
./build-fork.sh
# Build only: leave the APK in java/, no copy, no bump (toolchain proof / experiments)
./build-fork.sh --no-deliver
# Re-run ./configure (after changing configure flags or the deps)
./build-fork.sh --reconfigure
```

`build-fork.sh` does, in order: read `fork.properties` and `keystore.properties`; compute the
version; fetch the third-party ndk-build ports once into gitignored `emacs_deps/`
(`admin/download-android-deps.sh 64`, ~150 MB, network); `./autogen.sh autoconf`; `./configure`
(only when `config.status` is missing or `--reconfigure`); `make all -j$(nproc)` with the
`FORK_*` variables; copy + bump. **Never run bare `./autogen.sh`** — without the `autoconf`
argument it also installs upstream's `commit-msg` / `pre-commit` hooks into `.git/hooks`, which
would police our fork commits with GNU's ChangeLog rules.

### Toolchain

- JDK **11** at `/usr/lib/jvm/zulu11` — **JDK 11 only**. `configure.ac` compiles the Java side with
  `--release 7` (Emacs supports Android 2.2), which JDK 21 rejects outright. `build-fork.sh` sets
  `JAVA_HOME`/`PATH` itself; the host default `java` is JDK 11 too, but never rely on it.
- Android SDK at `~/android-sdk`: build-tools `37.0.0` (`aapt`, `d8`, `zipalign`, `apksigner`),
  `platforms/android-37.1/android.jar` (configure demands API 37 = CINNAMON_BUN), NDK
  `27.3.13750724` (r27: 16 KB page-size support), compiler
  `aarch64-linux-android29-clang` → one ABI, **arm64-v8a**, minSdk 29.
- Host tools: `autoconf`, `m4`, `makeinfo`, `tic`, `curl`, `git`, `shasum` (the deps script).
- No Gradle, no `local.properties`. The Android build is `java/Makefile.in` driven by the top-level
  `make all` (host Emacs → byte-compiled lisp → `cross/` NDK build of `libemacs.so` + the
  ndk-build deps → `java/` APK).
- Configure line (in `build-fork.sh`): `--with-shared-user-id=com.termux --without-android-debug
  --with-native-compilation=no` plus the full SourceForge dependency set (`gif gnutls harfbuzz jpeg
  png selinux sqlite3 tiff tree-sitter webp xml2`) from `--with-ndk-path`.

## Architecture (upstream GNU Emacs, Android port)

| Area | Where |
| --- | --- |
| Android build | `java/Makefile.in` (APK packaging, signing), `java/AndroidManifest.xml.in`, `java/INSTALL`, `admin/download-android-deps.sh` |
| Java side (activities, service, views, SAF, notifications) | `java/org/gnu/emacs/*.java` (40 files; `EmacsService`, `EmacsActivity`, `EmacsWindowManager`, `EmacsDocumentsProvider`, `EmacsNoninteractive` …) |
| Java resources | `java/res/` (`mipmap-v26/emacs_icon.xml`, `drawable/emacs_foreground.xml`, `values*/`, `xml/preferences.xml`) |
| C side of the port (JNI, GUI, VFS, fonts) | `src/android*.c`, `src/android*.h`, `src/sfnt*.c` (the in-tree TrueType renderer), `src/android-emacs.c` (the `emacs` wrapper binary) |
| Android terminal in Lisp | `lisp/term/android-win.el` |
| Cross build + ndk-build emulation | `cross/`, `m4/ndk-build.m4`, `exec/` (the `exec1`/`loader` helpers that run binaries on Android 10+) |
| Our layer | `java/Makefile.in` fork block, `build-fork.sh`, `fork.properties`, `CLAUDE.md`, `.claude/skills/`, `java/org/gnu/emacs/shiroikuma/` (the UI page, Export/Import, the tar engine, automation), `java/res/values/shiroikuma_*.xml`, `java/res/xml/shortcuts.xml` |
| Version literals (read only) | `configure.ac` `AC_INIT`, `java/AndroidManifest.xml.in` `Version-code:` trailer |

On the phone, `HOME` is `/data/data/shiroikuma.emacs/files` (the C side derives it from
`getFilesDir()`, so the rename needs no code); the Termux prefix is
`/data/data/com.termux/files/usr`, readable through the shared UID. Migrating from the stock app is
one `cp -a /data/data/org.gnu.emacs/files/. /data/data/shiroikuma.emacs/files/` from Termux.

## UI page (`org.gnu.emacs.shiroikuma`)

Framework Java 7 only (`--release 7`): anonymous classes, no lambdas/streams/`String.join`/`Base64`,
no AndroidX — SAF via `DocumentsContract` + `startActivityForResult`, dialogs via `android.app.AlertDialog`.

- **Sections / rows:** Export / Import (「Export / Import…」, 「Export directory」 red when unset, 「Last export」
  queried on resume on a background thread, 「Automation export」 default on, 「Use authorization token?」 default
  off, token row while on) · Emacs options (Restart with -Q, Restart with --debug-init, Delete dump file) ·
  Appearance (「Black system bars」 default on, 「Keep screen on while Emacs is in front」 default off; fonts /
  colours / cursor / keyboard are Emacs's own) · Reset (clears `shiroikuma_ui` + `shiroikuma_eximport` only).
- **Backup categories** (`EmacsBackup.Cat`): `settings` = "Settings" (the `shiroikuma_ui` prefs, type-tagged
  JSON) · `data.home` = "Home directory (~/.emacs.d, init files, packages)" (`tar -C <dataDir> -cf - files`,
  i.e. HOME = `/data/data/shiroikuma.emacs/files`). Both top-level, both default on. Restorable by hand:
  `unzip -p x.zip data.home.tar | tar -C /data/data/shiroikuma.emacs -x`.
- **Prefs files:** `shiroikuma_ui` (exported: `black_bars` bool, `keep_screen_on` bool) ·
  `shiroikuma_eximport` (device-local: `dir_uri` SAF tree, `pending_import` bool + `pending_items` — the
  relaunch marker) · `shiroikuma_automation` (device-local: `automation_enabled` true, `automation_require_token`
  false, `automation_token` 48 hex, lazily generated). All writes `commit()`.
- **Engine API the automation side uses:** `EmacsBackup.export (Context, Set<Cat>, OutputStream,
  Progress, Cancel)` → `Result`, `importZip (Context, File spool, Set<Cat>, Progress)` → `Result`,
  `categoriesIn (File)`, `manifestOf (File)`, `exportFileName ()`, `isExportFileName ()`, `humanSize ()`,
  `emacsRunning ()` (`EmacsService.SERVICE != null` — a home restore must refuse then), `Cat.parse (items,
  unknown)`, `Cat.defaults ()`; `SafDir.usableDir/createFile/rename/delete/stat/absolutePathOf`;
  `AutomationAuth.refuse (Context, token)`.

### Automation (sister-app contract v2, `~/git/shiroikuma-jiyusagyoban/sister-app-contract-backup-automation-hand-off.md`)

- **§1 broadcast door** — `StateExportReceiver` (exported, no `android:permission`): `EXPORT_STATE` gates
  (`AutomationAuth.refuse`), parses `items` (absent = `Cat.defaults ()`; unknown → `ERROR:unknown item <id>`),
  resolves the destination (`path` extra only with `Environment.isExternalStorageManager ()`, else exactly
  `ERROR:no-storage-access` — this app declares `MANAGE_EXTERNAL_STORAGE`, so no fall-back; no `path` →
  `SafDir.usableDir` else `ERROR:no-directory`), mints a job (`AutomationJobs.begin (replyId)`) and
  `startForegroundService (BackupService)` inside try/catch (`BackupService.startFailure`: the
  `ForegroundServiceStartNotAllowedException`-by-class-NAME + not-battery-exempt rule →
  `ERROR:no-foreground-start`, else `ERROR:cannot start export service: <Class>`). `LIST_CATEGORIES` and
  `CANCEL_EXPORT` (`AutomationJobs.cancelByReplyId`, silent, no reply) are answered in `onReceive`. Reply =
  fresh broadcast, `setPackage`, `FLAG_INCLUDE_STOPPED_PACKAGES`, `reply_id` echoed verbatim, `result` =
  `OK:<abs path>|<bytes>|<human>|<n> categories`; exactly one terminal reply per request.
- **§2a data door** — `AutomationProvider`: caller check `AutomationCallers.verify` (exact name → uid
  cross-check → pinned SHA-256; 応用管理 and 自由作業盤), then `AutomationAuth.refuse`; `describe` = header
  JSON from PackageManager + the `Cat` enum only (runs before `Application.onCreate`, never touches
  `EmacsNative`); `export`/`import` = `fd.dup ()` → `AutomationJobs.begin ()` → `BackupService.startDescriptorJob`
  in try/catch (close the dup, drop the job, return the refusal) → `OK:<job_id>`; `import` refuses
  `ERROR:Emacs is running; quit it (C-x C-c) or force-stop, then retry` when `data.home` is wanted and
  `EmacsBackup.emacsRunning ()`; `cancel` → `OK:cancelled`. Refusals are returned, never thrown.
- **`BackupService`** (one job at a time, notification channel `shiroikuma_backup`, id 4241, partial wakelock
  15 min): `onStartCommand` = read extras → `startForeground` inside try (refused → reply, close fd, `stopSelf`)
  → drain `HANDOVER` → silent early returns on stale ids → busy → worker. §1 writes `<name>.part` (MIME
  `application/octet-stream` on SAF, so the provider does not append `.zip`) and renames on completion; the
  partial is deleted in `finally` on any failure or cancel (`ERROR:cancelled`). §2a export writes into the
  descriptor through a counting stream (`OK:<bytes>|<human>|<n> categories`); §2a import spools to
  `getCacheDir ()/automation-import-<job>.zip`, validates, merges, `commit ()`s `shiroikuma_ui`, replies
  `OK:<n> categories restored; …`. Progress (`AutomationProgress`) only with a `progress_action` AND a
  `reply_package`: `reply_id` (+ `job_id` on the descriptor door), `app`, `item` = category id, `text`
  numbers-first (`区分 1/2 — Settings`, `ファイル n/N · x MB / y MB`), `current`/`total`, `unit`,
  `bytes`/`bytes_total`; ≥ 500 ms apart, final always, heartbeat re-sends the last line every 20 s.
- **Smoke test by hand** (warm process only — EMUI refuses the cold path from adb; §1 needs a configured
  export directory or `path` with All-files access): `am broadcast --include-stopped-packages -a
  shiroikuma.emacs.action.LIST_CATEGORIES -p shiroikuma.emacs --es reply_action X --es reply_package Y --es
  reply_id Z`. Real verification is 自由作業盤's 保存復元 batch and 応用管理's data backup.

## Hard rules

- **JDK 11 only** for this repo (`/usr/lib/jvm/zulu11`). Never point `JAVAC` at JDK 21.
- **Never rename the `org.gnu.emacs` Java package** — only the installed id differs (aapt rename).
  The JNI class names are compiled into `libemacs.so`; renaming would break startup and make every
  rebase a mass-conflict.
- **One keystore for the whole `com.termux` shared-UID family** (Termux, termux-api, termux-x11,
  termux-gui, this app): `~/.android-keystores/shiroikuma-emacs-termux.jks`. Upstream's tracked
  `java/emacs.keystore` stays as upstream has it but is **no longer what signs** — `keystore.properties`
  is. Changing the key later means uninstall + restore for everything on that UID.
- **Never commit/push unprompted.** Build, deliver, and stop; 白い熊 tests. Commit + push only on
  their explicit **"Push"** = commit + `git push --force-with-lease origin custom` (rebased history;
  `master` fast-forwards with a plain push).
- `keystore.properties`, `*.jks`, `emacs_deps/`, `.scratch/` and `.claude/settings.local.json` are
  gitignored — never commit them. `CLAUDE.md` and `.claude/skills/` **are** tracked (upstream's `.*`
  ignore rule is overridden for `.claude/`).
- **Always run `adb`, `scp` and `git status`/`git diff` with `dangerouslyDisableSandbox: true`**
  (the sandbox blocks adb's server socket and invents phantom untracked dot-files at the repo root).
- **After ANY functional change, build and deliver automatically** — the global `/after-build`
  standing authorization; never wait for "build it". Every build bumps `BUILD_NUMBER`; never
  overwrite or delete an older APK, in `~/tmp/` or on the phone. Always `run_in_background`, poll,
  never abandon it: measured 2026-09-13 on this 24-core host — one-time deps download ≈ 13 min
  (SourceForge is slow), configure ≈ 2 min, full make ≈ 6 min, warm no-op rebuild ≈ 35 s.
- On new upstream commits, run the **`upstream-new-version`** skill — it presents the
  proceed-gated Android-relevant change table **before** any rebasing, then ff-merges `master`,
  rebases `custom`, keeps `BUILD_NUMBER` running, and builds the next `+NNN`.
- **Rebase grep guard** (after every sync, before building): every site that must follow the
  installed id rather than the namespace —
  `grep -n '"org\.gnu\.emacs\|getShortClassName\|getIdentifier' java/org/gnu/emacs/*.java` and
  `git diff master..custom --stat` to confirm the fork layer is exactly what we expect.
- The stale `~/git/emacs` is a **reference clone**, never this repo. `etc/NEWS` is upstream's
  changelog and is never edited; ours is the root `CHANGELOG.md`.

### CHANGELOG.md is unified — our sections on top

Upstream ships no `CHANGELOG.md` (its history is `etc/NEWS`, left untouched), so the root
`CHANGELOG.md` is ours: one section per fork release, newest first,
`## 白い熊 GNU Emacs <tag> — <YYYY-MM-DD>`, each carrying (a) our changes since the previous release
and (b) an **"Upstream since <previous base sha>"** subsection distilled from the proceed-gate table
of the `upstream-new-version` runs in between. That is the merged changelog `/publish-version`
publishes with each release; the same text goes in the GitHub release notes.

## Commit convention — no Claude attribution

Do **not** add any `Co-Authored-By: Claude …` trailer, nor a "🤖 Generated with Claude Code" /
Anthropic-attribution line, to commit messages or PR bodies in this repo. End the message at the last
line of the body. This overrides the harness default. (Global rule: `~/.claude/CLAUDE.md`.)
